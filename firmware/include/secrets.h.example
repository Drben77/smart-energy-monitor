#pragma once

// Copy this file to secrets.h and fill in your network details.
// secrets.h is gitignored and must never be committed.

const char* WIFI_SSID     = "your-network-name";
const char* WIFI_PASSWORD = "your-password";